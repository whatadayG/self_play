hydra:
  run:
    dir: outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}
  sweep:
    dir: multirun/${now:%Y-%m-%d}/${now:%H-%M-%S}
    subdir: ${hydra.job.num}
  launcher:
    _target_: hydra._internal.core_plugins.basic_launcher.BasicLauncher
  sweeper:
    _target_: hydra._internal.core_plugins.basic_sweeper.BasicSweeper
    max_batch_size: null
    params: null
  help:
    app_name: ${hydra.job.name}
    header: '${hydra.help.app_name} is powered by Hydra.

      '
    footer: 'Powered by Hydra (https://hydra.cc)

      Use --hydra-help to view Hydra specific help

      '
    template: '${hydra.help.header}

      == Configuration groups ==

      Compose your configuration from those groups (group=option)


      $APP_CONFIG_GROUPS


      == Config ==

      Override anything in the config (foo.bar=value)


      $CONFIG


      ${hydra.help.footer}

      '
  hydra_help:
    template: 'Hydra (${hydra.runtime.version})

      See https://hydra.cc for more info.


      == Flags ==

      $FLAGS_HELP


      == Configuration groups ==

      Compose your configuration from those groups (For example, append hydra/job_logging=disabled
      to command line)


      $HYDRA_CONFIG_GROUPS


      Use ''--cfg hydra'' to Show the Hydra config.

      '
    hydra_help: ???
  hydra_logging:
    version: 1
    formatters:
      simple:
        format: '[%(asctime)s][HYDRA] %(message)s'
    handlers:
      console:
        class: logging.StreamHandler
        formatter: simple
        stream: ext://sys.stdout
    root:
      level: INFO
      handlers:
      - console
    loggers:
      logging_example:
        level: DEBUG
    disable_existing_loggers: false
  job_logging:
    version: 1
    formatters:
      simple:
        format: '[%(asctime)s][%(name)s][%(levelname)s] - %(message)s'
    handlers:
      console:
        class: logging.StreamHandler
        formatter: simple
        stream: ext://sys.stdout
      file:
        class: logging.FileHandler
        formatter: simple
        filename: ${hydra.runtime.output_dir}/${hydra.job.name}.log
    root:
      level: INFO
      handlers:
      - console
      - file
    disable_existing_loggers: false
  env: {}
  mode: RUN
  searchpath: []
  callbacks: {}
  output_subdir: .hydra
  overrides:
    hydra:
    - hydra.mode=RUN
    task:
    - algorithm.adv_estimator=grpo
    - algorithm.use_kl_in_reward=false
    - data.train_files=["/home/nickatomlin/georgiazhou/new_dialop/RL-matching/data/matching.parquet"]
    - data.val_files=["/home/nickatomlin/georgiazhou/new_dialop/RL-matching/data/matching.parquet"]
    - data.prompt_key=messages
    - data.train_batch_size=64
    - data.max_prompt_length=512
    - data.max_response_length=128
    - data.filter_overlong_prompts=false
    - data.truncation=error
    - data.return_raw_chat=true
    - actor_rollout_ref.model.path=Qwen/Qwen2.5-7B-Instruct
    - actor_rollout_ref.model.enable_gradient_checkpointing=true
    - actor_rollout_ref.model.use_remove_padding=true
    - actor_rollout_ref.actor.ppo_mini_batch_size=64
    - actor_rollout_ref.actor.ppo_micro_batch_size_per_gpu=1
    - actor_rollout_ref.actor.use_torch_compile=false
    - actor_rollout_ref.actor.fsdp_config.param_offload=true
    - actor_rollout_ref.actor.fsdp_config.optimizer_offload=true
    - actor_rollout_ref.rollout.name=sglang
    - actor_rollout_ref.rollout.multi_turn.enable=true
    - actor_rollout_ref.rollout.multi_turn.interaction_config_path=/home/nickatomlin/georgiazhou/new_dialop/RL-matching/configs/matching_interaction.yaml
    - actor_rollout_ref.rollout.engine_kwargs.sglang.attention_backend=flashinfer
    - actor_rollout_ref.rollout.prompt_length=512
    - actor_rollout_ref.rollout.response_length=128
    - actor_rollout_ref.rollout.n=1
    - actor_rollout_ref.rollout.tensor_model_parallel_size=2
    - actor_rollout_ref.rollout.gpu_memory_utilization=0.6
    - actor_rollout_ref.rollout.max_num_batched_tokens=512
    - actor_rollout_ref.rollout.enable_chunked_prefill=false
    - actor_rollout_ref.rollout.log_prob_micro_batch_size_per_gpu=1
    - actor_rollout_ref.ref.log_prob_micro_batch_size_per_gpu=1
    - critic.model.path=Qwen/Qwen2.5-7B-Instruct
    - critic.model.enable_gradient_checkpointing=true
    - critic.model.use_remove_padding=true
    - critic.ppo_micro_batch_size_per_gpu=1
    - reward_model.enable=false
    - trainer.project_name=verl_matching
    - trainer.experiment_name=grpo_mem_stage1_flashinfer_glibc_messages
    - trainer.logger=["console"]
    - trainer.n_gpus_per_node=2
    - trainer.nnodes=1
    - trainer.save_freq=0
    - trainer.test_freq=1
    - trainer.total_epochs=0
    - trainer.val_only=true
  job:
    name: main_ppo
    chdir: null
    override_dirname: actor_rollout_ref.actor.fsdp_config.optimizer_offload=true,actor_rollout_ref.actor.fsdp_config.param_offload=true,actor_rollout_ref.actor.ppo_micro_batch_size_per_gpu=1,actor_rollout_ref.actor.ppo_mini_batch_size=64,actor_rollout_ref.actor.use_torch_compile=false,actor_rollout_ref.model.enable_gradient_checkpointing=true,actor_rollout_ref.model.path=Qwen/Qwen2.5-7B-Instruct,actor_rollout_ref.model.use_remove_padding=true,actor_rollout_ref.ref.log_prob_micro_batch_size_per_gpu=1,actor_rollout_ref.rollout.enable_chunked_prefill=false,actor_rollout_ref.rollout.engine_kwargs.sglang.attention_backend=flashinfer,actor_rollout_ref.rollout.gpu_memory_utilization=0.6,actor_rollout_ref.rollout.log_prob_micro_batch_size_per_gpu=1,actor_rollout_ref.rollout.max_num_batched_tokens=512,actor_rollout_ref.rollout.multi_turn.enable=true,actor_rollout_ref.rollout.multi_turn.interaction_config_path=/home/nickatomlin/georgiazhou/new_dialop/RL-matching/configs/matching_interaction.yaml,actor_rollout_ref.rollout.n=1,actor_rollout_ref.rollout.name=sglang,actor_rollout_ref.rollout.prompt_length=512,actor_rollout_ref.rollout.response_length=128,actor_rollout_ref.rollout.tensor_model_parallel_size=2,algorithm.adv_estimator=grpo,algorithm.use_kl_in_reward=false,critic.model.enable_gradient_checkpointing=true,critic.model.path=Qwen/Qwen2.5-7B-Instruct,critic.model.use_remove_padding=true,critic.ppo_micro_batch_size_per_gpu=1,data.filter_overlong_prompts=false,data.max_prompt_length=512,data.max_response_length=128,data.prompt_key=messages,data.return_raw_chat=true,data.train_batch_size=64,data.train_files=["/home/nickatomlin/georgiazhou/new_dialop/RL-matching/data/matching.parquet"],data.truncation=error,data.val_files=["/home/nickatomlin/georgiazhou/new_dialop/RL-matching/data/matching.parquet"],reward_model.enable=false,trainer.experiment_name=grpo_mem_stage1_flashinfer_glibc_messages,trainer.logger=["console"],trainer.n_gpus_per_node=2,trainer.nnodes=1,trainer.project_name=verl_matching,trainer.save_freq=0,trainer.test_freq=1,trainer.total_epochs=0,trainer.val_only=true
    id: ???
    num: ???
    config_name: ppo_trainer
    env_set: {}
    env_copy: []
    config:
      override_dirname:
        kv_sep: '='
        item_sep: ','
        exclude_keys: []
  runtime:
    version: 1.3.2
    version_base: '1.3'
    cwd: /home/nickatomlin/georgiazhou/new_dialop/RL-matching
    config_sources:
    - path: hydra.conf
      schema: pkg
      provider: hydra
    - path: /home/nickatomlin/georgiazhou/new_dialop/verl/verl/trainer/config
      schema: file
      provider: main
    - path: ''
      schema: structured
      provider: schema
    output_dir: /home/nickatomlin/georgiazhou/new_dialop/RL-matching/outputs/2025-08-16/03-10-45
    choices:
      reward_model: dp_reward_model
      critic: dp_critic
      rollout@actor_rollout_ref.rollout: rollout
      ref@actor_rollout_ref.ref: dp_ref
      data: legacy_data
      actor@actor_rollout_ref.actor: dp_actor
      hydra/env: default
      hydra/callbacks: null
      hydra/job_logging: default
      hydra/hydra_logging: default
      hydra/hydra_help: default
      hydra/help: default
      hydra/sweeper: basic
      hydra/launcher: basic
      hydra/output: default
  verbose: false
